{
  "name": "plan-todo-kickoff-2026-04-24",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:smoke": "playwright test",
    "test:all": "npm test && npm run test:smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
